(function (window) {
    'use strict';

    var CommandScene = function () {
        this.graphics = {
        };
    };

    CommandScene.prototype.act = function () {
    };

    window.CommandScene = CommandScene;
})(this);
